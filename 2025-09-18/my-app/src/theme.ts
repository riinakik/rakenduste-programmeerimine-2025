import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#43741bff",
    },
    secondary: {
      main: "#7ab336",  
    },
       background: {
      default: "#f9f2e5", 
      paper: "#ffffff",  
    },
  },
});

export default theme;
